import React from "react";

type AppSwipeableCarouselProps = {
  slidesPerPage?: number;
  children: React.ReactNode;
  loop: boolean;
  spaceBetween?: number;
  showNavButtons: boolean;
};

export default AppSwipeableCarouselProps;
